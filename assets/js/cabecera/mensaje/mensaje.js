const typed=new Typed('.typed',{
    strings:['Reduce', 'Reutiliza', 'Recicla'],
    typeSpeed: 75, //Velocidad en milisegundos para colocar cada palabras
    startDelay: 300,
    backSpeed: 75,
    backDelay: 1500,
    loop: true,
    loopCount: false,
    showCursor: false,
}
);